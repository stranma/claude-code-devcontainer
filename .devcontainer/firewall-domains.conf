# Extra domains to whitelist in the devcontainer firewall.
# One domain per line. Lines starting with # are comments.
# Wildcard domains (*.example.com) are not supported (DNS cannot resolve them).
# Changes take effect on container restart.
#
# Examples:
# registry.npmjs.org
# dl.google.com
